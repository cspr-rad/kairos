// This crate can be used as a library to interact with Kairos Layer 2 Smart Contracts.
// The deployments crate exposes functions that both put a deploy and query the L1 (contract).

pub mod deployments;
