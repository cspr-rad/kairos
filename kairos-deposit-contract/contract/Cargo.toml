[package]
name = "contract"
version = "0.1.0"
edition = "2021"

[dependencies]
casper-contract = {git = "https://github.com/cspr-rad/casper-node", branch="kairos-feat-2.0", features=["std"]}
casper-types = {git = "https://github.com/cspr-rad/casper-node", branch="kairos-feat-2.0", default-features=false}
contract-types = {path = "../contract-types"}
serde-json-wasm = "1.0.1"

[build]
target = "wasm32-unknown-unknown"

[[bin]]
name = "deposit-contract"
path = "src/main.rs"
bench = false
doctest = false
test = false

[profile.release]
codegen-units = 1
lto = true

