### Diesel development tool installation
```
// Install postgresql-devel using your package manager (development dependencies for potsgresql)
cargo install diesel_cli --no-default-features --features "postgres"
```