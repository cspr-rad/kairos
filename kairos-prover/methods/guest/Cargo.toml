[package]
# TODO: Rename this package to change the method name from METHOD_NAME
name = "method_name"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[workspace]

[dependencies]
# If you want to try (experimental) std support, add `features = [ "std" ]` to risc0-zkvm
risc0-zkvm = { version = "=0.21.0", default-features = false }
kairos-circuit-logic = { path = "../../../kairos-circuit-logic", default-features = false, features = ["serde"] }
